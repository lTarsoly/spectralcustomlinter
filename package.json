{
  "dependencies": {
    "@stoplight/spectral-cli": "^6.15.0"
  },
  "scripts": {
    "lint": "spectral lint petstore.json --format stylish --verbose",
    "linttofile": "spectral lint petstore.json --format stylish -o lintresult.txt --verbose"
  }
}
